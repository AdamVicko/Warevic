

<?php if(App::admin()): //ako je auth admin?>
    <div class="d-grid gap-2 col-6 mx-auto" style="margin: 50px 0 0 0;">
        <a href="novi" class="btn btn-primary">New collection</a>
    </div>
<?php endif; ?>


<div class="table-responsive">
    <table class="table table-dark table-hover" style="margin: 50px 0 50px 0;">
        <thead>
            <tr>
                <th scope="col">Date</th>
                <th scope="col">Patient</th>
                <th scope="col">Oxygen Concentrator</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach($podaci as $p): ?>
                <tr>
                    <td><?=$p->datumPrikupa?></td>
                    <td><?=$p->imeprezime?></td>
                    <td><?=$p->serijskiKod?></td>
                    <td>
                        <a href="promjeni?id=<?=$p->sifra?>">Change</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>
