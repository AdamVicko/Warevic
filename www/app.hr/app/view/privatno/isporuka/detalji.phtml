
<fieldset class="border p-2">
  <legend class="float-none w-auto"><h1>Delivery Update</h1></legend>
    <form class="row g-3" id="registrationForm" action="<?php echo App::config('url') ?>isporuka/azurirajIsporuku/<?= $trenutniPodaciIsporuke->sifra ?>" method="post"><?//dovest ce me na promjenu sa svim podacima + sa sifrom?>
        <div class="d-grid col">
            <div class="col-md-11">
            </div>
            <div class="col-md-11">

                <label for="datumIsporuke">Delivery date</label>
                <input type="date" name="datumIsporuke" id="datumIsporuke" 
                    value="<?= preg_replace('/^(\d{2})\/(\d{2})\/(\d{4})$/', '$3-$2-$1', $trenutniPodaciIsporuke->datumIsporuke); ?>">

            </div>
            <div class="row" style="margin-top: 50px;">
                <div class="d-grid col-md-6">
                    <select name="pacijent" id="pacijent">
                    <?php foreach($pacijenti as $p)
                        { ?>
                            <?php 
                                $selected = "";
                                if($p->imeprezime === $trenutniPodaciIsporuke->imeprezime )
                                {
                                    $selected = "selected";
                                }
                            ?>
                            <option value="<?php echo $p->sifra?>" <?=$selected?> ><?php echo $p->imeprezime?></option>
                            <?php 
                        } ?>
                    </select>
                </div>
                <div class="d-grid col-md-6">
                <select name="koncentratoriKisika" id="koncentratoriKisika">
                    <?php foreach($koncentratoriKisika as $k)
                        { ?>
                         <?php 
                                $selected = "";
                                if($k->serijskiKod === $trenutniPodaciIsporuke->serijskiKod )
                                {
                                    $selected = "selected";
                                }
                            ?>
                            <option value="<?php echo $k->sifra?> " <?=$selected?> ><?php echo $k->serijskiKod?></option>
                            <?php 
                        } ?>
                            

                    </select>

                
                </div>
            </div>

        </div>
        <hr>
            <div class="row" style="margin-top: 50px;">
                <div class="d-grid col">
                    <button type="submit" class="btn btn-success" 
                            value="submit">Submit</button>
                </div>
                <div class="d-grid col">
                    <a href="<?=App::config('url')?>isporuka/odustani"
                        class="btn btn-danger">Cancel</a>
                </div>
            </div>
    </form>
</fieldset>

